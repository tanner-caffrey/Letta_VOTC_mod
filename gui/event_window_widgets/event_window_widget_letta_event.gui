hbox = {
	name = "event_window_widget_letta_event"
	layoutpolicy_horizontal = expanding
	margin = { 10 0 }

	state = {
        name = widget_show
        trigger_on_create = yes

		on_start = "[EventWindowData.CopyToClipboard]"

		on_finish = "[GetVariableSystem.Clear('letta_event_character')][GetVariableSystem.Clear('letta_event_type')][GetVariableSystem.Clear('letta_event_desc')]"
	}

	expand = {}

}
